version: 2

models:
  - name: stg_yellow_tripdata
    description: Cleaned, typed staging view over raw yellow taxi parquet.
    columns:
      - name: pickup_at
        tests:
          - not_null
      - name: trip_distance_miles
        tests:
          - not_null
