name: 'nyc_taxi_dbt'
version: '1.0.0'
config-version: 2

profile: 'nyc_taxi_dbt'

model-paths: ["models"]
seed-paths: ["seeds"]
clean-targets: ["target", "dbt_packages"]

models:
  nyc_taxi_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
    # mart_daily_summary (aggregation layer) added in a later class — see future/
